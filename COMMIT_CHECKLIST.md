# 提交前自检清单

每次执行 `git commit` 前，对照以下清单检查。不符合的不要提交。

## 核心原则：一个提交只做一件事

- [ ] **这个提交只做一件事吗？** 比如"新增其它收支收款方式"单独提交，"修复知识库搜索"单独提交
- [ ] **改动的文件都在同一个功能范围内吗？** 如果改了A功能的文件又改了B功能的文件，拆成两个提交
- [ ] **禁止把多个不相关的改动塞进同一个提交**（参考5月29日大提交埋下的两个Bug）

## 技术检查

- [ ] `npm run build` 无红色报错
- [ ] `npm run lint` 无 error（exit code 为 0）
- [ ] `npm run test:unit` 测试通过（如有新增/修改测试）

## 功能验证（必测3项）

- [ ] 登录正常
- [ ] 工单列表能正常加载
- [ ] 数据保存（增删改）正常

## 提交信息格式

```
类型: 描述
```

类型：
- `feat:` 新功能
- `fix:` 修复 bug
- `refactor:` 重构（不改功能）
- `chore:` 杂项（配置、依赖等）
- `docs:` 文档更新

## 什么时候该提交

**该提交的时候**：
- 一个功能点完成了，build 通过，测试通过

**不该提交的时候**：
- 改了两行就想提交
- 同时改了多个不相关的功能
- build 还有红色报错
- 还没测过登录、列表、保存
