# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.env*.local

# 备份与临时文件
*.bak
supabase/.temp/

# 数据表（Excel）
*.xlsx

# 构建与运行日志
build.log
build2.log
build3.log
server.log
logs/

# Android 构建产物和签名密钥
android/app/build/
android/.gradle/
android/app/my-release-key.jks

# 大型数据文件
model_for_api.sql

# Service Worker 构建产物（已由手动版本替代，不再忽略）
# public/sw.js

# 临时文件
tmp_*.html

# Claude 临时文件（注意：必须用 .claude/* 而不是 .claude/，否则下面的例外不生效）
.claude/*
!.claude/settings.json
!.claude/hook-post-edit.js
!.claude/hook-protect-critical.js
!.claude/skills/
!.claude/agents/
!.claude/rules/

# MCP 配置（含密钥，禁止提交）
.mcp.json
# 高危文件自动备份
*.bak

# 测试脚本
scripts/*
# 冒烟测试脚本属于长期设施，需要提交
!scripts/smoke-test.mjs
# 认证回归测试脚本同属长期设施
!scripts/auth-test.mjs

certificates
# 构建产物副本
.next-new/

# 数据库同步/导出的临时文件（含数据，禁止提交）——只锚定根目录，不影响 supabase/migrations/
/remote-auth.sql
/remote-data-only.sql
/remote-full-dump.sql
/remote_data.sql
/full-remote-data.sql
/full_remote_schema.sql
/insert-profiles.sql

# 本地化部署/数据同步脚本（本机用，不入库）
/backup-db.bat
/restore-db.bat
/setup-autostart.bat
/supabase-start.bat
/supabase-stop.bat
/use-local.bat
/use-remote.bat
/sync-auth.js
/sync-users.bat
/数据库本地化使用说明.md

# Supabase 本地开发的导入产物与备份
/supabase/migrations/20240101000000_full_schema.sql
/supabase/migrations_backup/
/supabase/snippets/

# 本地语义搜索模型缓存（体积大，不入库）
/.cache/

# Windows 系统文件
desktop.ini
smoke-test-failure.png
!scripts/smoke-test.mjs
# 认证回归测试脚本同属长期设施
!scripts/auth-test.mjs
!scripts/patrol-pages.mjs
!scripts/flow-tests.mjs
!scripts/realtime-test.mjs
!scripts/permission-test.mjs
.playwright-mcp/
