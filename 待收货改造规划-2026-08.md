# 待收货改造规划（2026-08-20 定稿）

> 本文档是"待收货"环节改造的**需求与施工蓝图**，基于用户 9 点需求 + 4 项拍板决策。
> 现状盘点见《采购流程梳理-2026-08.md》，异常处理流程见《采购流程思维导图.md》。

---

## 一、需求原点（用户 9 点）

1. 待收货时批量创建运单：发货人电话（关联供应商）、发货件数、运费金额、代收金额
2. 采购单已关联物流；运单电话命中供应商时，询问后确认关联
3. 采购单 ≠ 供应商的销售单（对方实发的那张单）
4. 到货逐个填实收数量，不符弹窗：少了→是否重新采购；多了→是否退货
5. 按实际到货的销售单生成待入库清单，确认后推进待入库
6. 确认收货的商品可以领料
7. 收货时不显示价格（采购价、销售价都不显示）
8. 商品不对打"待退货"标签
9. 运单可拍照存档

## 二、拍板决策（2026-08-20）

| # | 问题 | 决策 |
|---|---|---|
| 1 | 供应商销售单形态 | **不一定有纸质单**，可能是微信截图，也可能事后补 → 单号选填、照片（含截图）可后补 |
| 2 | 领料时机 | **确认收货即可领（急件直领）**：验货确认后技师就能领走；库存账务在"确认入库"事务里入库+直领出库一并完成 |
| 3 | 价格权限 | 待收货页价格**默认隐藏**，保留显示开关；但开关仅 **admin / boss / warehouse** 三角色可用，其余角色永远看不到 |
| 4 | 终端 | **手机为主**，电脑端也要；手机端同样能批量建运单、拍照（运单照/货物照/销售单截图）、**收货时定仓位（手填仓位或扫仓位二维码）** |

---

## 三、现状与差距（代码已核实）

| 需求 | 现状 | 差距 |
|---|---|---|
| 1 批量建运单 | ✅ 已有：表字段齐（tracking_no/phone/package_count/freight_amount/cod_amount/photos/supplier_name），待收货页有批量创建 | 照片字段**界面未接上传**；手机端无此功能 |
| 2 电话命中询问 | 🟡 输电话实时检索出供应商名 | 不会主动问"该供应商有 N 张待收货采购单，是否关联" |
| 3 供应商销售单 | ❌ 无此概念，系统只认自家采购单 | 需新建"到货确认单"承载单号+截图+核对结果 |
| 4 逐行数量+差异弹窗 | ✅ 已完整实现：订购 1 自动填 1；多/少/破损/错发 10 种分支全走 `receive_purchase_item` 事务 | 组织在"每张采购单"下，不是"抱着一包货逐件对" |
| 5 按实际到货生成待入库 | 🟡 采购单全部行处理完才整单转待入库 | 粒度要从"采购单"细化到"到货批次"，分批发货不再互相等 |
| 6 收货即可领料 | ❌ 现在必须确认入库后工单配件才标 is_arrived | 需把"已到货"标记提前到收货确认 |
| 7 隐藏价格 | 🟡 有全局 PriceVisibility 开关（*** 显示），但无角色维度，待收货页也没强制 | 加角色门禁：admin/boss/warehouse 才见开关 |
| 8 待退货标签 | ✅ 错发分支已能生成待退货记录 | 入口藏得深，做成一键 |
| 9 运单拍照 | 🟡 photos JSONB 字段早就在 | 界面完全没接，等于没有 |
| ⚠️ 手机收货页 | ❌ `/m/receiving` 状态值写成 `pending_receipt/partial_receipt`，合法值是 `submitted/partial_received` | **上线以来一直查不到数据，功能完全失效**（梳理问题 #2） |

---

## 四、目标流程

```
① 收到货（货运站取件/送货上门）
   → 手机批量录运单：单号 + 发货人电话 + 件数 + 运费 + 代收 + 拍照
   → 电话命中供应商 → 弹问:「××供应商有 N 张待收货采购单,是否关联到这张运单?」

② 拆包验货（全程不显示配件价格）
   → 选运单建「到货确认单」：拉出该供应商所有在途采购行
   → 有供应商销售单就填单号+拍照/传微信截图（没有就跳过,以后能补）
   → 逐件操作：
       填实收数量 + 选仓位（手填或扫仓位码）+ 可拍货物照
       数量对   → 确认
       少了     → 弹窗:补采购(自动加"少发补货"待采购) / 不要了
       多了     → 弹窗:退货(打"多发退货"标签) / 折价留下 / 免费留下
       货不对   → 一键打「待退货」标签
       破损     → 换货 / 折价 / 不要了

③ 确认到货单
   → 按本批实际收到的东西生成「待入库清单」（不再等整张采购单齐）
   → 工单件此时标"已到货"→ 技师可立即领料（急件直领）

④ 待入库 → 确认入库（一个数据库事务）
   → 按行上已定仓位加库存；未定的补选
   → 记应付款、运费分摊、工单配件状态
   → 已被直领的件:同事务内完成 入库+出库(库存净不变,流水留痕)

⑤ 待退货 → 已退货（现有流程不变）
```

---

## 五、数据结构设计（二期新建）

### 5.1 新表：`arrival_receipts` 到货确认单

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| receipt_no | text | 单号 `DH-日期-序号` |
| waybill_id | uuid→logistics_waybills | 关联运单（本地供货可空） |
| supplier_id | uuid→suppliers | 供应商 |
| supplier_order_no | text 可空 | 供应商销售单号（可后补） |
| photos | jsonb | 销售单照片/微信截图（可后补） |
| status | text | `receiving` 验货中 → `confirmed` 已确认 → `inbounded` 已入库 |
| created_by / created_at / confirmed_at | | 操作留痕 |

### 5.2 新表：`arrival_receipt_items` 到货明细

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| arrival_id | uuid→arrival_receipts | 所属到货单 |
| purchase_order_item_id | uuid 可空 | 对应的采购行；**采购单上没有的货（错发/多发）为空** |
| part_id | uuid 可空 | 关联库存配件 |
| part_name_snapshot | text | 配件名快照（防采购行后续变动） |
| expected_qty | int | 应收数量（采购行剩余未收） |
| received_qty | int | 实收数量 |
| handling | text | 差异处理：normal / 少发补货 / 少发弃货 / 多发退货 / 折价留下 / 免费留下 / 破损换货 / 破损折价 / 破损退货 / 错发换货 / 错发退货 |
| location_id | uuid 可空 | **收货时定的仓位**（入库直接用） |
| photos | jsonb | 货物照片（破损取证等） |

两表均建索引 + RLS（与采购主表同级：admin/boss/warehouse 可写）。

### 5.3 老表加列

- `purchase_order_items` 加 `arrival_item_id`（可空）→ 该行由哪条到货明细收的，留痕
- `work_order_item_parts.is_arrived` 回写时机提前：到货确认时置 true（现在等入库）

---

## 六、写操作/事务设计

| 操作 | 方式 | 说明 |
|---|---|---|
| 批量创建运单 | 新 RPC `create_waybills` | 逐行电话匹配供应商名；返回命中结果供前端弹问关联 |
| 运单关联采购单 | 新 RPC `link_waybill_orders` | 询问确认后批量挂 waybill_id |
| 运单/货物照片上传 | 复用 `/api/upload` + imageCompress 300KB | 手机调起相机 |
| 建到货确认单 | 新 RPC `create_arrival_receipt` | 选运单+供应商，拉入在途采购行 |
| 逐行收货处理 | **复用现有 `receive_purchase_item`** | 10 种异常分支逻辑不重写，只多记 arrival_item_id |
| 确认到货单 | 新 RPC `confirm_arrival_receipt` | 事务：到货单转 confirmed + 涉及行推进待入库 + 工单配件标已到货 |
| 确认入库 | 改造 `complete_purchase_inbound` | 输入从"采购单 id"改为"到货单 id"；行上已有仓位的直接用；直领件同事务出库 |
| 待退货记录 | 现有逻辑不变 | 破损/错发/多发件自动生成 |

> 原则：二期不重写已稳定的行级异常逻辑，`receive_purchase_item` 是最大资产，只在外面包"到货单"这层组织容器。

---

## 七、界面改造清单

### 手机端（主战场）
1. **修复 `/m/receiving` 状态值 bug**（一行改，功能从失效恢复）
2. 运单批量创建（手机友好表单，逐行：单号/电话/件数/运费/代收/拍照）
3. 电话命中供应商 → 弹问关联待收货采购单
4. 到货验货页：逐件填数量、选仓位（手填 + 调 BarcodeScanModal 扫仓位码）、拍照
5. 差异弹窗、一键"待退货"标签
6. 价格一律不显示（手机端不做价格开关）

### 电脑端
7. 待收货页价格默认隐藏；开关仅 admin/boss/warehouse 可见可用（PriceVisibilityContext 加角色维度）
8. 运单照片上传查看、供应商销售单号/截图补录
9. 与手机端同一套到货单工作台（数据互通）

### 配套
10. 仓位二维码生成与打印（若无现成功能则新增，便于收货扫码定仓位）

---

## 八、分期实施

| 期 | 内容 | 风险 |
|---|---|---|
| **一期：抢修+快赢**（不动数据流） | ① 修手机收货页 bug ② 运单拍照接通 ③ 批量建运单上手机+电话命中弹问关联 ④ 待收货页价格角色门禁 ⑤ 错发一键打待退货标签 | 低，全是界面层+小 RPC |
| **二期：到货确认单**（动数据流） | ⑥ 建 2 张新表 ⑦ 待收货改版为"运单→到货单"工作台 ⑧ 收货定仓位（手填+扫码）⑨ 按到货单生成待入库、入库事务改造 ⑩ 急件直领（到货确认→可领料，入库事务内完成直领出库） | 中，需迁移在途单 |
| **三期：账务深化**（可缓） | ⑪ 到货单参与供应商对账 ⑫ 代收/运费与应付款勾稽 | 低 |

### 存量兼容（二期上线时）
- 在途采购单（submitted/partial_received）**走老路径收完为止**，新收货一律走到货单
- 不迁移历史数据，采购单状态机不动，只把"待入库"的来源从采购单切换为到货单

---

## 九、风险与注意

1. **入库事务改造是二期最大风险点**：`complete_purchase_inbound` 现在按采购单跑 10 张表，改按到货单跑后要在测试库反复验证库存/应付款分摊
2. **急件直领的账实一致**：直领件在入库事务里"即入即出"，若确认入库前又撤销收货，需把已领料的直领件拦截（提示"已领料，不能撤销"）
3. **价格门禁别只挡界面**：二期到货单的查询 RPC 直接不 select 价格列，网络层也不回传（一期先做到界面层）
4. **手机拍照走 `/api/upload`**：该路由刚修过白名单（PR #138），压缩用现有 imageCompress
5. **仓位扫码复用 BarcodeScanModal**：注意关闭时必须 stopScan()（历史坑）
6. **多会话并行开发必须用各自 worktree**（防互冲）
