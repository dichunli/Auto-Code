/* 移动端今日考核入口（底部导航"考核"）
 * 直接复用桌面端今日考核页：懒生成今日记录 + 首屏服务端查询 + 自检/核查弹窗
 * 全是同一套逻辑，此处只做路由注册——套在 /m 布局里（有底部导航和认证守卫）。
 * 桌面端改逻辑（如自检计分、核查改判）时本页自动跟随，无需同步两处。 */
export { default } from "@/app/behavior/checks/page";
