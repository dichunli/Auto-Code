/* ============================================================
 * 全站上传限制统一口径
 *
 * 2026-09-14 用户拍板：视频单文件最大 4GB，大文件自动走分片上传
 * （chunkedUpload.ts，5MB 一片、断点续传），替代原先各处散写的
 * 4GB / 1080MB / 500MB / 100MB 等多种口径。
 *
 * 注意：个别场景有显式的更严限制（如质检凭证 100MB+60 秒），
 * 属于业务规则，不在此收敛范围。
 * ============================================================ */

/** 视频单文件上限（MB） */
export const 视频最大MB = 4096;

/** 视频单文件上限（字节） */
export const 视频最大字节 = 视频最大MB * 1024 * 1024;

/** 超过此体积自动走分片上传（字节） */
export const 分片阈值字节 = 100 * 1024 * 1024;
