# 采购流程梳理（2026-08）

> 本文档是对采购模块代码实现侧的全面盘点，基于 main 分支 `5b4a3039`（PR #129）。
> 业务流程的**需求设计稿**见《采购流程思维导图.md》（本文不覆盖它）。
> 梳理时间：2026-08-15；梳理方式：全代码库只读审计，不改任何业务代码。

---

## 一、一句话总览

采购模块管理"配件从缺货到上架"的全过程：**工单缺件/备货 → 询价报价 → 客户确认 → 发起采购 → 收货（含异常处理）→ 入库（加库存、记应付款）→ 退货（冲减应付款）**。2026-08-11 起核心写操作已全部事务化（一个操作一个数据库事务，中途失败整体回滚，不会留下半成品数据）。

---

## 二、业务流程总览

### 2.1 配件需求的两个来源

```
来源 A：维修工单缺件（主流）
  工单 → 维修项目 → 配件分支（work_order_item_parts）
    → 待询价（还没填采购价）
    → 可生成「询价单」发给供应商微信扫码报价（链接 3 小时有效）
    → 待报价（有采购价、没填销售价）
    → 待确认（两价齐全，等客户拍板）
    → 客户同意 → 待采购

来源 B：备货（无工单，2026-08-15 起走暂存）
  安全库存补货弹窗（库存低于下限的配件）
  自定义采购弹窗（手工搜索/新建配件）
    → 先存进「采购暂存表」(custom_purchase_staging)
    → 显示在「待采购」页，与工单配件混在一起勾选
```

### 2.2 采购主流程（待采购 → 已入库）

```
待采购：勾选同一供应商的配件（工单件+暂存件可混选）→ 选物流方式
  →「发起采购」→ 生成采购单（状态=submitted，单号 CG-日期-序号）
  → 工单配件行回写"已采购"，暂存行从暂存表删除

待收货：采购单到货后逐行登记，10 种处理动作：
  ├─ 正常收货（数量对、无破损）
  ├─ 数量对但破损 → 换货（自动补一条"破损补发"待采购）/ 折价 / 不要了
  ├─ 数量对但发错 → 换货（补一条"错发换货"待采购）/ 不要了
  ├─ 多发 → 多出部分退货 / 折价留下 / 免费留下
  └─ 少发 → 少发部分再补货（补一条"少发补货"待采购）/ 不要了
  全部明细处理完 → 采购单自动转「待入库」(pending_storage)

待入库：「确认入库」一个数据库事务完成 10 件事：
  建入库单(RK-单号) → 逐行入库明细 → 库存数量原子增加+刷新采购价
  → 仓位记录 → 批次记录 → 库存流水 → 记应付款(debit)
  → 采购单转「已入库」(completed) → 工单配件标"已到货"
  → 破损/错发/多发件自动生成「待退货」记录
  ※ 运费按数量占比分摊进每个配件的入库成本

已入库：如需撤销，「退回待入库」整单回滚（⚠️ 目前是唯一没事务化的高危操作）

待退货 → 已退货：
  路径A「生成采退单」：按供应商汇总生成采退单(CT-单号) + 记应收冲减(credit) ← 有账
  路径B 单条「标记完成」：只改状态，不生成单、不记账 ← 无账（口径分裂，见问题清单）
```

### 2.3 供应商往来账（钱）

| 操作 | 账目类型 | 含义 | 产生方式 |
|---|---|---|---|
| 采购入库 | debit 应付 | 欠供应商的钱增加 | 确认入库时自动记 |
| 生成采退单 | credit 应收冲减 | 退货抵掉一部分欠款 | 生成采退单时自动记 |
| 付款 | payment | 实际付给供应商 | 手工"记一笔"或外包付款 |
| 退款 | refund | 供应商退回的钱 | 手工"记一笔" |

**注意：没有核销机制**——付款就是一条流水，不与具体哪张入库单的应付勾稽；余额靠前端加总计算。另有一套独立的 `accounts_payable` 表只被外包和财务页使用，与采购这套账互不相通（见问题 17）。

---

## 三、页面与菜单入口

### 3.1 导航菜单「采购管理」三个入口

| 菜单 | 路由 | 内容 |
|---|---|---|
| 采购流程 | `/procurement` | 主工作台，11 个页签（见下） |
| 入库单 | `/inbound-orders` | 入库单列表+详情+打印（只读） |
| 采退单 | `/return-orders` | 采退单列表+详情+打印（只读） |

### 3.2 `/procurement` 主工作台的 11 个页签

| 页签 | 组件 | 数据来源 | 主要操作 |
|---|---|---|---|
| 待询价 | PartBranchStatusList | 工单配件行（无采购价） | 填价、行内编辑、生成询价单 |
| 待报价 | 同上 | 有采购价无销售价 | 填销售价 |
| 待确认 | 同上 | 两价齐全等客户 | 记录客户意见 |
| 待采购 | PendingPurchaseList | 工单配件行 + **采购暂存表** | 勾选→发起采购；安全库存/自定义采购两个弹窗 |
| 待收货 | PendingReceiptList | 采购单(submitted/partial_received) | 逐行收货、10 种异常处理、运单 |
| 待入库 | PendingStorageList | 采购单(pending_storage) | 确认入库(事务)、退回待收货 |
| 已入库 | CompletedStorageList | 采购单(completed) | 查看、退回待入库（⚠️未事务化） |
| 待退货 | PendingReturnList | supplier_return_records(pending) | 生成采退单、标记完成、批量撤销 |
| 已退货 | CompletedReturnList | supplier_return_records(completed) | 撤销（⚠️未事务化） |
| 入库单/采退单 | 链接跳转 | — | 跳到独立页面 |
| 询价单 | 链接跳转 `/quote-sheets` | supplier_quote_sheets | 复制链接/采用/作废 |

页签角标带 Realtime 实时订阅（采购单、退货记录、询价单等变动时自动刷新数字）。

### 3.3 其他相关独立页面

| 路由 | 功能 | 备注 |
|---|---|---|
| `/procurement/orders` | 采购订单总列表（全状态平铺） | 状态标签缺 2 个（问题 11） |
| `/procurement/new` | 手工新建采购单 | 供应商按车型智能排序 |
| `/procurement/[id]` | 采购单详情+逐行部分收货 | 部分收货只累加数量，不加库存 |
| `/suppliers` | 供应商列表/新建/编辑/详情 | 保存走事务（主表+5 张关联表） |
| `/supplier-transactions` | 供应商往来账 | 手工"记一笔" |
| `/supplier-returns` | 退货记录列表 | 与"采退单"是两回事 |
| `/quote-sheets` | 询价单管理 | 内部用 |
| `/quote/[token]` | 供应商免登录报价页 | 凭链接 token，3 小时有效 |
| `/reports/procurement` | 采购报表 | 供应商/配件 Top10、月度汇总、采退率、XLSX 导出 |
| `/m/receiving` | 手机收货页 | ⚠️ 状态值写错，永远无数据（问题 2） |

---

## 四、数据表关系（15 张表）

```
suppliers 供应商 ─┬─< purchase_orders 采购单 ──< purchase_order_items 采购明细
                  │        │                          ├─> parts 库存配件
                  │        │                          └─> work_order_item_parts 工单配件行
                  │        ├─ logistics_waybills 运单 / logistics_companies 物流
                  │        └─< inbound_orders 入库单 ──< inbound_order_items 入库明细
                  │                                        └─> warehouses 仓库/仓位
                  ├─< supplier_transactions 往来账(应付/付款/冲减)
                  ├─< supplier_return_records 退货记录 ──> purchase_return_orders 采退单
                  │                                                    └─< purchase_return_order_items
                  ├─< supplier_quote_sheets 询价单 ──< supplier_quote_items 报价值
                  ├─< supplier_contacts 等 5 张关联表(联系人/分类/件名/品牌/车型)
                  └─< custom_purchase_staging 采购暂存(2026-08-15 新增)

parts 库存配件 ──< part_batches 批次(入库批次, reference_id=采购单id)
             ──< part_stock_locations 分仓库存
             ──< inventory_logs 库存流水(reference=入库单id)
             ──< custom_purchase_staging 暂存(可空关联)
```

要点：
- 入库单与采购单是 **1对1**（`inbound_orders.purchase_order_id`），采购单删除时入库单保留（SET NULL）
- 批次记录的 `reference_id` 存的是**采购单** id（不是入库单 id），语义容易误伤
- 往来账的 `reference_type/reference_id` 是逻辑关联（无数据库外键），指向入库单或采退单
- `accounts_payable` 表（应付账款）**采购流程不用它**，只有外包和财务页在用

---

## 五、写操作链路（Server Action → RPC 事务）

2026-08-11 事务化改造后，核心写操作统一为：页面按钮 → Server Action（验证登录）→ RPC 数据库事务函数（多表写入，失败整体回滚）。

| 页面操作 | Server Action | RPC 事务函数 | 事务内写的表 |
|---|---|---|---|
| 发起采购（3 个入口） | `创建采购单` | `create_purchase_orders` | 采购单、明细、回写工单配件"已采购" |
| 添加到待采购（暂存） | `添加采购暂存` | 无（单表插入） | 采购暂存表 |
| 收货登记（10 种异常） | `提交收货处理` | `receive_purchase_item` | 明细、克隆补货分支、采购单状态、运单签收 |
| 撤销收货 | `撤销收货处理` | `revoke_purchase_receipt` | 同上（反向） |
| 部分收货（详情页） | `部分收货登记` | `receive_purchase_item_partial` | 明细数量累加、状态推进 |
| 少发弃货删行 | `删除采购明细` | `delete_purchase_item` | 明细、工单配件行、空采购单 |
| **确认入库** | `确认采购入库` | `complete_purchase_inbound` | **10 张表**（入库单/明细/库存/仓位/批次/流水/应付款/采购单/工单配件/退货记录） |
| 退回待收货 | `退回待收货` | `revoke_pending_storage` | 删补货分支/退货记录、状态回退 |
| 批量撤销退货 | `批量撤销退货` | `revoke_supplier_returns` | 整单入库回滚（约 10 张表） |
| 生成采退单 | `生成采退单` | `create_purchase_return_orders` | 采退单/明细/退货记录/应收冲减 |
| 保存供应商档案 | `保存供应商档案` | `save_supplier_full` | 主表+5 张关联表（全量重建） |
| 标记退货完成 | `完成退货记录` | 无（单表更新） | 退货记录 |
| 改客户意见/单据名称 | `更新工单配件客户意见` 等 | 无（单/双表更新） | 工单配件行 |

所有 RPC 函数统一：`SECURITY DEFINER` + 函数内登录兜底 + 2026-08-14 起角色门禁（仅 admin/boss/warehouse 可调）。

询价报价一组（`src/app/quote/actions.ts`）：内部操作走登录客户端；供应商公开报价走 service role（绕 RLS），带限流（查询 60 次/时）。

### 仍是客户端直写的次要路径（单表或弱关联，风险较低）

- 待采购页：数量行内编辑、移除暂存行、撤销客户意见
- 待收货页：运单创建/关联
- 待询价等页：行内编辑配件关联（usePartLinking）、图片上传
- 往来账手工"记一笔"（供应商往来账页、供应商详情页）
- 发起采购成功后**删除暂存行**（失败只打日志，暂存行会残留——见问题 18）

---

## 六、状态机

### 6.1 采购单状态（数据库允许 8 个值）

```
            （draft 草稿：2026-08-15 起已无产生入口，仅历史数据）
submitted 已提交 → partial_received 部分收货 → pending_storage 待入库 → completed 已入库
     ↑                  ↑ 撤销单行可回退            │ 退回待收货/撤销退货回退到 submitted
     └──────────────────┴────────────────────────────┘
（approved/fully_received/cancelled 三个状态无任何代码使用，属死状态）
```

### 6.2 工单配件行的平行状态（由字段推导，展示用）

待询价(无采购价) → 待报价(无销售价) → 待确认(客户意见=pending) → 待采购(同意且未采购) → 待收货 → 待入库 → 待领料 → 已领料

由 `src/lib/partWorkflow.ts` 统一推导；`is_purchased`/`is_arrived` 两个布尔值由采购 RPC 回写。

---

## 七、历史坑（已发生过的生产事故）

| 时间 | 事故 | 原因 | 现状 |
|---|---|---|---|
| 2026-05 ~ 08 | 采购入库的应付款记录**一条都没有** | 0519 迁移给往来账表加 reference 两列实际未生效，入库记应付款整句失败被静默吞掉 | 0812 迁移已补列修复 |
| 2026-05 | 一批采购单卡住无法收货 | 0514 迁移注释误判"status 无 CHECK 约束"，与 0501 实际定义矛盾 | 0517 两个迁移补救 |
| 2026-08 前 | 收货/入库中途失败留半成品数据 | 客户端多步连环写无事务 | 0811 事务化改造已收编（除 2 处遗漏，见问题 1/9） |

教训沉淀：① 迁移执行后需校验是否真生效；② `console.warn` 吞错的模式在资金数据上造成过真实损失。

---

## 八、问题清单（按严重度分级）

### 高危（错账/资金/功能失效）

| # | 问题 | 位置 | 后果 |
|---|---|---|---|
| 1 | **已入库「退回待入库」仍是客户端 10 步连环写**，无事务；库存先读再写不原子；且入库单等表只有 admin 能删，非 admin 执行时删除静默 0 行 | `CompletedStorageList.tsx:91-251` | 库存已扣、单据还在的错账；并发时库存错乱 |
| 2 | **手机收货页状态值写错**：过滤条件用了 `pending_receipt/partial_receipt`，但合法采购单状态是 `submitted/partial_received` | `src/app/m/receiving/page.tsx:18` | 手机端收货永远查不到数据，功能完全失效 |
| 3 | ~~draft 采购单无流转出口，安全库存补货单卡死~~ | — | ✅ **已于 2026-08-15 修复**（PR #128/#129：补货改走暂存表，统一发起 submitted 单） |
| 4 | **采购主表 RLS 仍是"登录即全权"**：角色门禁只加在 10 个 RPC 函数里，接待/技师可直接走 PostgREST 改采购单、删供应商，绕过门禁 | `purchase_orders`/`purchase_order_items`/`suppliers`/`supplier_return_records` 等表策略 | 权限双保险缺一半 |
| 5 | **双入库路径并存无防重**：待入库行可跳"入库登记"手工入库（只加库存，不建入库单、不记应付款、不推进采购单），与"确认入库"互不知晓 | `PendingStorageList.tsx:527` ↔ `inventory/actions.ts:95` | 同一配件库存可能重复增加 |

### 中危（口径不一致）

| # | 问题 | 位置 | 后果 |
|---|---|---|---|
| 6 | 两处撤销（批量撤销退货、退回待入库）都**不回退 is_arrived** | RPC `revoke_supplier_returns`、`CompletedStorageList.tsx` | 撤销后工单配件仍显示"已到货"，误导 |
| 7 | 退货**单条"标记完成"不生成采退单、不记往来账**；走"生成采退单"才有 | `SupplierReturnsContent.tsx:109`、`actions.ts:292` | 同一动作两条路径，财务口径分裂 |
| 8 | **供应商应付余额公式方向可疑**：`应付+付款-退货冲减-退款`，付款越多欠款越大 | `SupplierDetailClient.tsx:172` | 欠款数字可能一直是错的，需业务复核 |
| 9 | 「撤销已退货（采退单）」客户端 5 步连环删，无事务 | `CompletedReturnList.tsx:69-127` | 中途失败留半成品 |
| 10 | 手工"记一笔"往来账仍是客户端直插，未走 Server Action 验证 | `SupplierTransactionsContent.tsx:136`、`SupplierDetailClient.tsx:138` | 绕过登录验证规范 |

### 低危（卫生问题）

| # | 问题 | 说明 |
|---|---|---|
| 11 | 采购订单列表页状态标签缺 `pending_storage`/`completed` | 这两种状态显示英文原文（`orders/page.tsx:5-21`） |
| 12 | `schema.sql` 严重陈旧 | 缺 4 张新表、采购单状态还是旧 6 值；按它重建库会得到错误结构 |
| 13 | RPC 角色门禁用"读定义→字符串锚点替换"注入 | 今后重写这 10 个函数体会**静默丢失门禁**，无机制防止 |
| 14 | 死状态×3、死字段×2、命名易混 | approved/fully_received/cancelled；return_reason/not_arrived_reason；`purchase_returns`（库存退货）与 `purchase_return_orders`（采退单）极易混淆 |
| 15 | 供应商详情页退货记录全表拉取后前端过滤 | 数据量大后拖慢首屏（`suppliers/[id]/page.tsx:73-76`） |
| 16 | 外包模块按 `description ILIKE '%单号%'` 模糊删财务记录 | 单号子串撞车会误删（`MobileItemEditor.tsx:1765`） |
| 17 | **双轨应付款**：采购记 `supplier_transactions`，外包/财务页用 `accounts_payable` | 两本账互不相通，采购应付款不会出现在财务应付页 |
| 18 | 发起采购成功后删暂存行，失败只打日志 | 暂存行残留→下次还显示在待采购，可能重复采购（有在途排除兜底，风险有限） |

---

## 九、改造建议路线图（供拍板，本次只梳理不动手）

### 批次 1：错账风险收口（最优先）
1. 已入库「退回待入库」收编成 RPC 事务（可大量复用 `revoke_supplier_returns` 的整单回滚逻辑）
2. 双入库路径加防重（确认入库后/已有入库单的配件禁止手工入库登记，或手工登记也建入库单记应付款）
3. 两处撤销补回退 `is_arrived`

### 批次 2：数据正确性
4. 修手机收货页状态值（一行改动，功能从完全失效恢复）
5. 复核供应商应付余额公式（需先和业务确认正确口径）
6. 统一退货完成口径：单条"标记完成"是否也应记往来账？（需业务拍板）

### 批次 3：安全收口
7. 采购主表 RLS 收紧到与 RPC 门禁一致（admin/boss/warehouse 才能写）
8. 手工"记一笔"收编 Server Action
9. 外包删财务记录改用 reference 精确匹配

### 批次 4：未完工作 / 新功能
10. **供应商自助报价**：设计已定稿（询价链接 3 小时有效、提交可改、采用锁死），分支 `feat/supplier-quote-link` 待联调实施
11. **`worktree-procurement-ops` 工作树处置**：它的 3 个提交不在 main 历史中，但经逐文件核对，功能主体（三阶段 Tab 交互规则、单据名称全 Tab 可编辑、待采购客户意见可改、两个迁移文件）**其实已在 main 中**（由其他 PR 以不同提交哈希带入，如 PR #123），同名文件剩余差异仅数行量级。建议：逐文件 diff 核对剩余差异、确认无遗漏后**直接废弃该工作树**——不要走合并（同一批文件两边演进过，合并必冲突）
12. 卫生清理：状态标签补齐、死状态死字段清理、`purchase_returns` 改名防混淆、`schema.sql` 更新或标注废弃

---

## 十、附：本次梳理方法

- 3 个只读探索代理并行摸底：采购单模块 / 入库·退货·往来账·供应商 / 数据库层迁移
- 远程新合并 PR #128/#129（采购弹窗增强、暂存统一发起流程）单独增量补查
- 全部结论基于代码实读，关键处附 `文件:行号` 引用，可逐条复核
