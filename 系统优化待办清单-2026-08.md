# 系统优化待办清单（2026-08-27 整理）

> 来源：《系统体检报告-2026-08-21.md》+ 8-21~8-27 整改过程。
> 用法：每项独立，挑着做；做完把状态改成 ✅ 并注明 PR 号。新会话接手时先读本文件。
> 原则：**一次只做一项**，走分支+PR，build+lint 过了再合并。

## 状态图例
- ⬜ 未开始做
- 🔄 进行中（注明谁在做）
- ✅ 已完成

---

## 一、安全类（建议优先，都是小改动）

### ✅ 1. 照片接口防"冒充手机"（2026-08-29 完成，PR #172）
- **问题**：`src/app/api/media/[...path]/route.ts:11-17,128-133` 靠 User-Agent 判断是否 APP，改 UA 即可免登录下载员工身份证照片、工单照片
- **改法**：APP 环境也校验 Bearer token（参照 `src/app/api/upload/route.ts` 已有的 token 模式）
- **工作量**：半天（含 APP 端真机验证）

### ✅ 2. 打印功能名称转义（2026-08-29 完成，PR #173）
- **问题**：`src/app/inventory/InventoryTable.tsx:610,653`、`src/app/tools/management/components/ToolQrCode.tsx:64`、`LocationQrCode.tsx:62` 把配件/工具名直接拼进 `document.write` 的 HTML；恶意名称可经 xlsx 导入带入
- **改法**：拼接前转义 `&<>"`（加个 `转义HTML()` 小工具放 src/lib，配单元测试）
- **工作量**：1 小时

### ✅ 3. 搜索词统一清理（2026-08-29 完成，38 处，PR #173）
- **问题**：`.or(\`name.ilike.%${query}%\`)` 拼接未清理 `,%_().` 字符（PostgREST 过滤器注入，只读、需登录，风险低）
- **改法**：`src/lib/` 加 `清理搜索词()` 统一去掉特殊字符，所有 `.or()` 调用点替换；典型位置 `src/components/CustomerSearchDropdown.tsx:76`、`src/app/vehicles/page.tsx:18`
- **工作量**：半天

### ✅ 4. 安全小尾巴（2026-08-29 完成，PR #174/#175）
- `training_categories` 表被显式 DISABLE RLS（`supabase/migrations_20260615_training_categories_rls_fix.sql:6`）→ 改回 ENABLE + 登录可读/管理员可写策略
- `vehicles_dedup_backup_20260813` 备份表（无 RLS）→ 确认去重无后遗症后 DROP
- `src/app/api/delete-media/route.ts:36-58` 不校验文件归属 → 加"仅管理员或文件业务关联人可删"
- `/api/media` 私有图片响应头 `Cache-Control: public, max-age=31536000`（行 224）→ 非 quote 目录改 `private`
- 上传白名单含 gif/webm/Office/pdf，宽于规范（`api/upload/route.ts:47-51`）→ 规范与实际二选一统一（建议更新 CLAUDE.md 规范到实际口径）

---

## 二、数据正确性类

### ✅ 5. 客户端直写库收编（2026-08-27~29 全部完成，9 个 PR）
- **结果**：422 处客户端直写全部收口为 Server Action / RPC 事务，按域分 9 批：涉钱涉库存三处（PR #160）、财务域（#161）、工单域全量（#162/#163）、客户/预约/车辆域（#164）、行为考核/培训域（#165）、采购看板组件（#166）、基础资料管理域（#167）、杂项收尾（#168）、长尾清零（#169）
- 多步写全部事务化（预收款/退库/外协/配件合并/名称合并等 10 个新 RPC），身份字段一律取服务端 user.id

### ✅ 6. 考核检查页"一打开就写库"（2026-08-29 完成，PR #169）
- **结果**：考核记录改为每日定时任务生成（`/api/cron/generate-behavior-checks`，CRON_SECRET 校验，幂等可重跑），页面渲染和客户端翻页都只读不写库；Windows 计划任务"汽修管家-考核记录每日生成"已注册（每天 23:00）

### ✅ 7. 工单 action 身份字段（2026-08-29 完成，PR #171）
- **问题**：`src/app/work-orders/actions.ts:429` `submitter_id: item.submitter_id || user.id` 接受客户端传入，可伪造提交人
- **改法**：改为只取 `user.id`；顺手检查同文件其他写函数有无同类问题
- **注意**：改完跑 `npm run test:auth`（动的是工单 action）

### ✅ 8. 采购权限收紧落地（2026-08-29 完成，PR #176 + Dashboard 迁移已执行）
- **前置**：前端先按角色隐藏接待岗的"关联运单"入口（否则 tighten 后接待岗点了报 42501，体验差）
  - 位置：`src/components/PendingReceiptList.tsx:760-787` 的运单关联 UI + `src/app/logistics/actions.ts:75` 的 `关联运单到采购单或配件`
- **执行**：`supabase/migrations_20260816_purchase_rls_tighten.sql`（内容与正式版一致，Dashboard 跑）
- **验证**：用接待岗账号点一遍待收货页，确认无关联入口、其他功能正常
- **工作量**：半天

---

## 三、架构/性能类

### ⬜ 9. 采购看板 7 个列表 + 16 个页面改服务端首屏
- **问题**：这些页面在客户端 useEffect 里首次拉数据，SPA 软导航时 session 未就绪会导致页面空白（历史病根）
- **采购看板 7 组件**：`src/components/PendingPurchaseList.tsx:182`、`PendingReceiptList.tsx:172`、`PendingStorageList.tsx:182`、`CompletedStorageList.tsx:87`、`PendingReturnList.tsx:112`、`CompletedReturnList.tsx:65`、`PartBranchStatusList.tsx:153`
- **16 个页面**：17vin-billing、finance/other/payment-methods、finance/other/stats、inventory/batches、knowledge/categories、m/other、m/receiving/waybills、m/reception、reports/procurement、settings、settings/segment-dictionary、settings/synonyms、tools/management、tools/management/records、members/[id]、procurement/[id]
- **改法**：套用 `src/app/work-orders/page.tsx` + `WorkOrdersContent.tsx` 的成熟模式（服务端首屏 + Client Content）
- **注意**：PendingStorageList 有 DeepSeek 的"销售单对照区"半成品背景，开工前先确认 main 上该文件最新状态
- **工作量**：分批做，每批 3-4 个页面一个 PR

### ⬜ 10. 22 个文件手写防抖统一为 useDebounce
- **清单**（体检已逐一核实）：m/reception/new(4处)、service-items/[id]/edit(4)、service-items/new(4)、suppliers/SupplierForm(4)、parts/new/components/SpecialPricingSection(4)、vehicles/new/VehicleForm(3)、vehicles/[id]/edit(2)、part-brands/PartBrandsContent:69（同文件主搜索用Hook次搜索手写，自相矛盾）、part-brands/[id]/edit、part-names/[id]/edit(2)、part-names/new(2)、part-names/BatchLinkDialog、part-specifications/[id]/edit、part-specifications/PartSpecificationsContent、parts/new/components/PartNumberField、PricingSection、hooks/usePartFormInit:626、tools/management、AddWorkOrderItemPartModal、OutsourceModal、PartBranchEditor、PartGroupHeader
- **改法**：`const 防抖词 = useDebounce(原始词, 300)` + 查询逻辑放 useEffect 依赖防抖词
- **工作量**：半天，可一个 PR 全改（模式统一）

### ⬜ 11. 搜索联想组件收敛
- **现状**：part_names 联想块逐字复制到 13 个文件；10 个搜索下拉组件并存共 2426 行（VehicleModelSearch 291 行 vs VehicleModelSelector 688 行功能高度重叠）
- **改法**：收敛到通用 SearchDropdown（`src/components/SearchDropdown.tsx` 已有雏形 175 行），逐调用点替换
- **工作量**：1-2 天，建议和第 10 项一起做（同一批文件）

---

## 四、测试类

### ⬜ 12. 核心业务补测试
- **零覆盖的核心流程**：客户创建、工单开单（结算和入库回滚已有 DB 层测试 ✅）
- **无测试的金额敏感函数**：`src/lib/commission.ts`（提成）、`src/lib/partPriceResolver.ts`（十级价格优先级，CLAUDE.md 核心规则）、`src/lib/permissions.ts`、`src/lib/workOrderData.ts`
- **参照**：`src/lib/supabase/__tests__/`（client 模块 953 行测试的写法）、`supabase/tests/`（DB 集成测试模式）
- **目标**：工具函数测试覆盖率从 44% 提到 80%+

---

## 五、流程/卫生类

### ⬜ 13. 迁移管理流程（防再踩"0820 顺序雷"）
- 建"已执行迁移台账"：数据库里加一张 `migration_log` 表（文件名+执行时间+执行人），或在 Supabase 建 dashboard 可查的表；每次执行迁移后插一行
- 同日多文件命名加 `_a/_b/_c` 或时分后缀（0820 一天 6 个文件字母序≠开发序，真实踩过雷）
- `supabase/` 根目录 15 个散落 SQL（fix_*/seed_*/cleanup_*）归入 `supabase/archive/`
- `supabase/migrations_20260814_behavior_check_records_unique_待清理后执行.sql` 全文在注释里（执行无效果），需要时先取消注释再执行，执行后转正文件名

### ⬜ 14. MobileItemEditor.tsx（3907 行）拆分
- **时机**：下次要改它的功能时顺手拆，不专门停工期拆
- **拆法**：按区块拆成文件级子组件（配件搜索/项目编辑/价格计算），严禁内嵌子组件（ESLint 红线）

### ⬜ 15. 小配置一批（一个 PR 收编）
- `eslint.config.mjs` 三条红线显式写死（现在靠插件 recommended 继承，升级依赖可能静默降级）：`@typescript-eslint/no-explicit-any`、`react-hooks/rules-of-hooks`、`react-hooks/static-components`
- `next.config.ts` `serverActions.bodySizeLimit: "2gb"` → `200mb`（业务上限视频 100MB）
- PM2 装日志轮转：`pm2 install pm2-logrotate`
- `src/app/m/page.tsx:81` 的 `/m/test` 测试页链接移出生产导航
- 删 `@mantine/hooks`（src 下 0 引用；`@mantine/core` 是 blocknote 的间接依赖，别删）
- `next.config.ts` 的 `allowedDevOrigins` 写死内网 IP 192.168.1.75，换网络环境记得改（只加注释提醒）

### ⬜ 16. 根目录杂散文件清理
- 可删：`check-rpc-exists.js`（功能已被 `check-pending-migrations.js` 覆盖）、test-vin17-get/ocr-full/post/post2.js 4 个调试脚本、`casstime-menu-try.png`、`menu-list.json`（这两个是 DeepSeek 会话的，先问它）
- 保留：`check-pending-migrations.js`（部署前自查工具）、`检查-迁移执行状态.sql`（Dashboard 验证工具）
- 体检配套文件（体检报告、修复/验证 SQL、分析脚本）建议归档到 `docs/health-check-202608/` 子目录

---

## 六、运维遗留（不是代码）

### ⬜ 17. 重启电脑后删 supplier-inbound 残留目录
- 路径：`.claude/worktrees/supplier-inbound`（注册已清，目录被进程占用删不掉，重启后 `rm -rf` 即可）

### ⬜ 18. 待确认后清理的 git 残留
- `procurement-ops` worktree + `worktree-procurement-ops` 分支：含 3 个未进 main 的提交（`b4cc7bad` 补录迁移文件、`98287336` 三阶段Tab交互、`10fa2476` 单据名称可编辑）——确认这些功能已在 main 用别的方式实现后删除
- 3 个有独有提交的分支：`feat/behavior-photos`（1 个）、`feat/mobile-checks-entry`（1 个）、`feat/tool-management`（10 个，落后 main 564 个提交基本废了）
- `feat/supplier-order-inbound`：0 独有提交，但当时被 git 保护拦截——确认远程分支也无价值后 `git branch -D` 强删
- `deepseek-待入库销售单-半成品备份.patch`（根目录，87 行）：DeepSeek 会话的过时半成品，确认正式版（PR #150/#151）已覆盖后删

### ✅ 19a. Next.js 16.2.4 → 16.3.3 安全补丁（2026-08-29 完成，PR #177，APP 真机验证通过）

### ⬜ 19b. @supabase/ssr 升级（认证层第二步存储收敛，原第 19 项后半）
- 单独挑稳定期做，必须跑 test:auth 认证回归

---

## 七、数据库与工程补充项（2026-08-27 Claude 诊断合并）

> 以下来自 2026-08-27 独立诊断，是本清单原本没有的项（与上文重叠的已省略）。合并自已删除的 docs/技术债清单。

### ⬜ 20. 数据库安全与卫生

| 问题 | 现状 | 优先级 | 改法 |
|------|------|--------|------|
| ⚠️ `fn_waybill_freight_payable` 触发器匿名可执行 | 是触发器函数（挂 `logistics_waybills`），被安全顾问标为 anon 可执行 | 高 | `REVOKE EXECUTE ... FROM anon, authenticated`（触发器本身不受影响） |
| 泄露密码保护关闭 | Supabase Auth 未开 HaveIBeenPwned 检查 | 中 | 用户在 Supabase 后台手动开启 |
| `pg_trgm` 扩展装在 public schema | 安全顾问提示移到独立 schema | 低 | 移扩展有风险，可暂缓评估 |
| 约 160 个未使用索引 | 多数索引未被查询使用 | 低 | 观察后再逐个清理，不能直接删 |

### ⬜ 21. 工程健康补充

| 问题 | 现状 | 优先级 | 改法 |
|------|------|--------|------|
| `strictNullChecks` 关闭 | `tsconfig.json` 显式关闭，1300+ 处"可能为 null" | 中 | 分批恢复 + 修空值告警 |
| 无 CI | 无 `.github/workflows`，测试只能手动跑 | 低 | 接入 CI 或 git 提交钩子 |
| `browser-test.mjs` 未接入 npm scripts | package.json 无 test:browser 入口 | 低 | 补 scripts 入口 |

---

## 已完成归档（2026-08-21 ~ 08-27）

- ✅ 12 张敏感表 RLS 启用、ignoreBuildErrors 移除、client.ts 门面化（8 月上旬）
- ✅ 0820 函数版本雷修复（confirm_arrival_receipt 档案价兜底，2026-08-21）
- ✅ 12 个增长型列表页分页（PR #154，2026-08-26 部署）
- ✅ 车辆 VIN/车牌数据库唯一锁（PR #155，2026-08-27）
- ✅ ngrok.exe 等仓库瘦身（DeepSeek PR #153）
- ✅ 32 个已合并分支 + 3 个旧 worktree 清理（2026-08-26）
