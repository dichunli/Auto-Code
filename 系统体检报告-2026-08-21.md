# 系统体检报告（2026-08-21）

> 体检方式：5 个并行审计代理（安全 / 代码质量 / 架构与数据流 / 数据库 / 认证与测试运维），全部结论附文件证据。
> 对比基线：2026-08-05 体检（72 分）、2026-06-17 稳定性体检。

---

## 一、总体结论

**综合评分：约 76 分**（较 8 月 5 日的 72 分上升 4 分）

上次体检的四大问题**已修复三个半**：

| 上次问题 | 现状 |
|---|---|
| 12 张敏感表 RLS 未启用 | ✅ 已修复（161 张表仅 1 张临时备份表遗漏） |
| `ignoreBuildErrors` 关闭类型安检 | ✅ 已移除（2026-08-06） |
| `client.ts` 662 行臃肿 | ✅ 已重构为 37 行门面 + 6 模块（2026-08-10），另有 953 行测试 |
| 测试覆盖薄弱 | ⚠️ 改善中（15 → 29 个测试），但核心业务仍无测试路径 |

**当前三大系统性风险**：

1. **"写了没执行、执行顺序错"的迁移管理流程问题** —— 8 月 20 日 3 个迁移文件同名函数版本递进，按文件名顺序执行会静默丢失最新逻辑；8 月 21 日迁移未执行但代码已上线引用新列。
2. **124 个客户端组件绕过 Server Action 直接写库（约 390 处）** —— 涉钱的预付款、涉库存的退库都在其中，是"偶发保存失败"的病根土壤。
3. **8 个增长型列表无分页 + 5 个列表硬 limit 截断** —— 数据超量后静默丢失，供应商列表永远只显示前 20 个。

---

## 二、分维度详情

### 🛡️ 维度 1：安全（评分：良好，2 个中危）

**合格的项**（已验证无问题）：
- 161 张表 RLS 覆盖完整，核心敏感表（客户/车辆/工单/配件/库存/财务/工资）全部启用
- service_role key 仅 2 处使用，均在服务端；`.env.local` 未被 git 追踪
- 23 个 Server Action 文件 100% 有登录校验；工资等敏感 action 有角色校验
- SQL 注入 0 处（全部参数化）；知识库 XSS 有白名单消毒
- 供应商免登录报价通道：随机 token + 有效期 + 限流，设计规范

**中危 1：照片接口可被"冒充 APP"绕过登录**
- 文件：`src/app/api/media/[...path]/route.ts:11-17, 128-133`
- 问题：通过 User-Agent 判断是否 APP 环境，任何人改 UA 即可免登录下载员工身份证照片、客户照片、工单照片
- 建议：APP 环境也校验 Bearer token（上传接口已有此模式可复用）

**中危 2：打印窗口存在注入风险**
- 文件：`src/app/inventory/InventoryTable.tsx:610,653`、`src/app/tools/management/components/ToolQrCode.tsx:64`、`LocationQrCode.tsx:62`
- 问题：配件/工具名称直接拼入 `document.write` 的 HTML，恶意名称（可经 xlsx 批量导入带入）可在打印窗口执行代码，读取本机登录状态
- 建议：拼接前对名称做 `<>&"` 转义

**低危**：38+ 处 `.or()` 搜索拼接未清理特殊字符（建议加统一 `清理搜索词()` 工具）；`training_categories` 表被显式禁用 RLS；车辆去重备份表无 RLS（建议清理后直接删表）；`/api/delete-media` 不校验文件归属；私有图片响应头 `Cache-Control: public` 建议改 `private`；上传白名单（含 gif/webm/Office）宽于项目规范，规范与实际二选一统一。

---

### 💻 维度 2：代码质量（评分：良好，红线全守住了）

**已根治**：`any` 类型 0 处；组件内定义子组件 0 处；eslint 红线均为 error；类型安检已开。

**高危 1：124 个客户端组件直接写库约 390 处**（占全部客户端组件 35%）
- 违反"核心写操作必须走 Server Action"规范，是偶发 401/42501 保存失败的直接土壤
- TOP 违规：`MobileItemEditor.tsx`（21 处）、`LogisticsContent.tsx`（13）、`PartMergeDialog.tsx`（12）、`OutsourceModal.tsx`（11）
- 典型危险证据：
  - `CreateMaintenanceWrapper.tsx:61-239` —— 删+插五连写全在客户端，中途失败产生半截数据
  - `AdvancePaymentDropdown.tsx:105-170` —— **预付款金额客户端双写，涉钱**
  - `AppointmentActions.tsx:55-109` —— 客户+车辆+工单+预约四连写
- 注：采购域（PR #116）已事务化收编，工单/客户/预约/行为考核域仍是客户端直写

**高危 2：页面一打开就写数据库**
- 文件：`src/app/behavior/checks/page.tsx:47-98`
- 问题：GET 请求（刷新、爬虫、预渲染）都会触发 insert 考核记录，仅靠唯一约束冲突兜底
- 建议：改为 Server Action 由按钮触发，或挪到定时任务

**中危**：
- 22 个文件手写 `setTimeout` 防抖（规范要求统一 `useDebounce`），`PartBrandsContent.tsx` 同一文件里主搜索用 Hook、次搜索手写，自相矛盾
- 搜索联想代码逐字复制到 13 个文件；10 个搜索下拉组件并存共 2426 行（`VehicleModelSearch` 291 行 vs `VehicleModelSelector` 688 行功能高度重叠）
- `MobileItemEditor.tsx` 已达 3907 行，比上次体检进一步膨胀
- eslint 三条红线规则靠插件 recommended 继承而非显式声明，升级依赖时可能静默降级，建议在 `eslint.config.mjs` 显式写死

**低危**：源码目录残留 `client.ts.bak`（30KB）和 `middleware.ts.bak`，建议删除（Git 已有历史）。

---

### 🏗️ 维度 3：架构与数据流（评分：中等，丢数据问题最 urgent）

**合规面**：约 34 个列表页已是"服务端首屏 + Client Content"成熟模式；Server Action 规范整体健康；全局状态无混乱（仅 5 个 Context）。

**高危 1：8 个增长型列表完全无分页，超 1000 条静默丢数据**
- `follow-ups`（回访）、`procurement/orders`（采购订单）、`supplier-returns`、`outsource-orders`、`behavior/tasks`、`behavior/checks`、`reminders`、`logistics`（运单）
- Supabase 默认 1000 条上限，超出后无任何提示地丢数据

**高危 2：5 个列表硬 limit 截断**
- `suppliers/page.tsx:11` —— `.range(0, 19)` **供应商永远只显示前 20 个**，超过后新增供应商不可见
- `appointments`（限 100）、`operation-logs`（限 200）、`procurement/arrivals`（限 100）、`behavior/score`（限 30）

**高危 3：采购看板 7 个列表组件 + 16 个展示页面用 useEffect 首屏加载**
- 采购看板：`PendingPurchaseList`、`PendingReceiptList`、`PendingStorageList`、`CompletedStorageList`、`PendingReturnList`、`CompletedReturnList`、`PartBranchStatusList`
- 16 个页面：财务其他收支/统计、库存批次、知识分类、手机端收货/接车、工具管理、设置页等
- 这正是"SPA 导航时 session 未就绪导致页面空白"的历史病根模式，工单/客户/库存页已有成熟合规样本可套用

**中危**：
- `work-orders/actions.ts:429` 身份字段 `submitter_id: item.submitter_id || user.id` 接受客户端传入，可伪造提交人（其余核心 action 全部合规）
- 10 个文件超 1000 行（最大 `MobileItemEditor.tsx` 3907 行）
- 测试页 `/m/test` 挂在手机端生产导航菜单（`src/app/m/page.tsx:81`）
- 3 个陈旧 worktree 占盘约 900MB（attendance 290M、procurement-ops 429M 已定废弃、procurement-review 183M 已合并）

**低危**：`serverActions.bodySizeLimit: "2gb"` 远超视频 100MB 业务上限，建议收紧到 200MB；PM2 日志无轮转，建议装 pm2-logrotate；144 个组件平铺 `src/components/`（工单域约 60 个），建议按域分目录。

---

### 🗄️ 维度 4：数据库（评分：中等，流程问题 > 文件质量）

**进步**：8 月后迁移质量明显提升（幂等、权限收尾、回滚预案齐全）；代码引用的 36 个 RPC 在迁移中全部有定义，无孤儿引用；新函数权限处理规范（REVOKE + auth.uid() + 角色门禁）。

**高危 1：8 月 20 日 3 个迁移同日同名函数版本递进，文件名序与开发序相反**
- `confirm_arrival_receipt`、`complete_arrival_inbound` 在 `arrival_receipts`（10:09）/ `arrival_settlement`（17:26）/ `arrival_extra_items`（18:35，最新）中各有一份实质不同的定义
- 按文件名排序执行后，最终生效的是中间版——**"补录采购单外货品按档案价兜底"逻辑丢失，补录件入库单价变 0，库存成本失真且无报错**
- 验证方法：SQL Editor 跑 `SELECT pg_get_functiondef('confirm_arrival_receipt(uuid)'::regprocedure) LIKE '%purchase_price FROM parts%';`，返回 false 即中招
- 建议：把最新版函数单独抽一份 `migrations_2026082X_arrival_functions_final.sql` 固化

**高危 2：woip_rpc 6 个函数执行状态未验证**
- `migrations_20260819_woip_rpc.sql` 在待执行目录，但 `parts-actions.ts:42-84` 通过动态拼接函数名调用全部 6 个（静态 grep 抓不到，`check-rpc-exists.js` 也漏了它们）
- 若未执行，工单配件行删除分支/批量加件/克隆/标记采购在手机端和桌面端全部报 PGRST202
- 建议：把 woip 6 函数 + arrival 7 函数补进探测脚本跑一次

**高危 3：8 月 21 日迁移未提交未执行，代码已引用新列**
- `migrations_20260821_waybill_exempt.sql`（未 git add）给采购单/明细加了 7 个列，已提交的代码 `PendingReceiptList.tsx` 直接查询这些列
- **迁移不执行就部署，待收货页直接整页报错**
- 部署顺序必须先执行迁移再部署代码

**高危 4：待收货页运单"创建+关联"两步写无事务，且与 RLS 收紧冲突**
- `PendingReceiptList.tsx:760-787` 先插运单再更新采购单，第二步失败留半成品
- `migrations_20260816_purchase_rls_tighten.sql`（待执行）把采购单 UPDATE 收紧到 admin/boss/warehouse 后，接待岗（receptionist）连新增的 Server Action `关联运单到采购单或配件` 也会被拦（该 action 内部仍是用户会话直写，非 RPC）
- 执行 tighten 前需先拍板：接待岗是否需要关联运单

**中危**：
- 5 月历史遗留：3 处裸 `CREATE TABLE vehicle_models`（无 IF NOT EXISTS），说明当时执行记录不可靠，建议建"已执行迁移台账"表
- `migrations_20260813_vehicles_unique_待清理后执行.sql` 悬挂：44 组重复 VIN + 2 组重复车牌清理完才能执行，**车辆唯一约束至今只有前端单保险**（明细已生成：`docs/重复车辆明细-20260813.csv`）
- `migrations_20260814_behavior_check_records_unique_待清理后执行.sql` 全文在注释里，执行无效果，状态极易误解
- 命名双轨（CLI 格式 3 个 + 日期前缀格式 240 个并存）；同日多文件无序号后缀（0820 一天 6 个）正是高危 1 的成因，建议同日加 `_a/_b/_c` 后缀

**低危**：15 个散落 SQL（fix_/seed_/cleanup_ 等）建议归入 `supabase/archive/`；车辆去重备份表建议清理后删表。

**事务化覆盖现状**：采购 15 RPC + 工单配件行 6 RPC + 到货确认 4 RPC + 运费/外协/退货各 1，核心多步写基本收编。剩余客户端直写仅 2 处（供应商退货单行 insert 属有意保留；待收货运单关联见高危 4）。

---

### 🔐 维度 5：认证 / 测试 / 运维（评分：认证大幅改善，测试仍最弱）

**认证已根治**：`client.ts` 门面化拆分（clientCore 84 行 / sessionCookie 128 / sessionStorage 87 / clientWriteMarker 45 / sessionInjection 139 / loginHealth 165），补丁函数各归其位；认证回归测试（`scripts/auth-test.mjs`）用真实浏览器覆盖 5 个历史事故场景，失败自动截图。

**高危 1：写操作 session 兜底覆盖率仅约 5%**
- 125 个客户端直接写库文件，仅 6 个调用 `确保有session`
- TOP 风险（全部涉钱/涉库存/涉核心数据）：
  - `AdvancePaymentDropdown.tsx`（预收款金额）
  - `finance/transactions/new/page.tsx`（财务记账）
  - `inventory/returns/new/page.tsx`（采购退库）
  - `WorkOrdersContent.tsx:95`（删工单）、`customers/DeleteButton.tsx:53`（删客户）
  - `reception/new`（接车单）、`inspection/new`（质检单）、`reimbursement`（报销单）
  - `CustomerImportExport.tsx`（批量导入客户）、`VehicleSearchAdd.tsx`（车辆绑定）

**高危 2：核心业务零测试路径**
- 客户创建、工单开单无任何测试；200 个页面仅 2 个组件级测试
- 关键金额工具函数无测试：`commission.ts`（提成）、`partPriceResolver.ts`（十级价格优先级，CLAUDE.md 明列核心规则）、`permissions.ts`、`workOrderData.ts`
- 工具函数测试覆盖率约 44%（21/48）

**中危**：
- `ngrok.exe`（32MB）被 git 追踪，每次 clone 全量拉取，`.gitignore` 未排除
- 35 个本地分支中 30 个已合并可清理（3 个被 worktree 占用需先清 worktree）
- 根目录约 10 个杂散调试文件（check-rpc-exists.js、test-vin*.js、中文名 bat 等）

**低危**：
- 版本锁定名不副实：除 next/react 外全部 `^` 范围，靠 lock 文件兜底
- `@mantine/hooks` 在 src 下 0 引用可删；`@mantine/core` 仅被 blocknote 间接依赖，删除前需验证
- `deploy.bat` 与文档一致（停服→删 .next→构建→启动），末尾 `pause` 已知

---

## 三、修复路线图（按优先级）

### 🚨 第一梯队：本周必须处理（会影响线上数据正确性）

| # | 事项 | 工作量 |
|---|---|---|
| 1 | ~~执行 `migrations_20260821_waybill_exempt.sql`~~ ✅ **已验证安全（2026-08-21 探测）**：7 个新列数据库已存在，git 已提交（22cabeb7），无需操作 | 已完成 |
| 2 | ~~验证 0820 批次函数版本~~ ✅ **已修复（2026-08-21）**：探测发现 `confirm_arrival_receipt` 生效的是中间版（缺档案价兜底），已用《修复-confirm_arrival_receipt-档案价兜底.sql》覆盖为最新版并验证通过；其余 5 项全部最新版。历史补录件是否有 0 成本批次待用户确认 | 已完成 |
| 3 | 把 woip 6 函数 + arrival 7 函数补进 check-rpc-exists.js 跑一次，确认数据库里都有 | 20 分钟 |
| 4 | ~~供应商列表 `.range(0, 19)` 改真分页~~ ✅ **复核为误报**（供应商页本就有完整分页）；实际修复的是 12 个无分页页面——**PR #154 已合并**（运单/采购订单/预约/操作日志/到货单/考核3页/退货/外协/回访/提醒） | 已完成 |
| 5 | 执行 `purchase_rls_tighten` 前拍板：接待岗是否需要关联运单 | 需用户决策 |

### 🔧 第二梯队：本月处理（防止问题扩大）

| # | 事项 | 工作量 |
|---|---|---|
| 6 | ~~8 个无分页列表补分页~~ ✅ **已随 PR #154 一并完成**（12 页统一改造含此 8 页） | 已完成 |
| 7 | 涉钱涉库存三处改 Server Action 或加 session 兜底（预付款/财务记账/采购退库） | 半天 |
| 8 | `behavior/checks/page.tsx` 渲染期写库改为按钮触发 | 2 小时 |
| 9 | 采购看板 7 个列表改服务端首屏（套用工单页成熟模式） | 1 天 |
| 10 | `/api/media` UA 绕过修复 + 打印 XSS 转义 | 半天 |
| 11 | `work-orders/actions.ts:429` 身份字段改为只取服务端 user.id | 10 分钟 |
| 12 | ~~车辆 44 组重复 VIN 清理→唯一约束~~ ✅ **已完成（2026-08-27）**：全量扫描确认重复已提前清零（0 组），`vehicles_vin_unique` + `vehicles_plate_number_unique` 两个唯一索引已执行生效，迁移文件转正（PR #155） | 已完成 |

### 🧹 第三梯队：择期清理（减负）

| # | 事项 |
|---|---|
| 13 | ngrok.exe 移出 git 追踪 + .gitignore |
| 14 | 清理 30 个已合并分支 + 3 个陈旧 worktree（释放 900MB）+ 2 个 .bak 文件 |
| 15 | 22 个文件手写防抖统一为 useDebounce；13 处搜索联想收敛成通用组件 |
| 16 | 补核心业务测试：客户创建、工单开单、提成计算、十级价格解析 |
| 17 | 建"已执行迁移台账"表；迁移同日多文件加 _a/_b/_c 后缀 |
| 18 | MobileItemEditor.tsx（3907 行）按区块拆分 |
| 19 | eslint 三条红线显式写死；bodySizeLimit 收紧；pm2 日志轮转；/m/test 移出生产导航 |

---

## 四、给用户的通俗总结

这次体检好比给车子做年检：**发动机（安全/认证）状态良好，但三个地方需要尽快修**：

1. **"账本"问题**：有几个清单页（供应商、回访、运单等）只能显示固定条数，超出的数据不是丢了，是**显示不出来**——供应商超过 20 家后，新录的根本看不见。
2. **"施工顺序"问题**：8 月 20-21 日的数据库升级脚本有几个**还没在数据库里执行**，但新代码已经在用它们了，下次部署可能直接报错；还有三个脚本如果按错误顺序执行，会悄悄丢掉"补录件按档案价入库"的逻辑。
3. **"保险丝"问题**：涉及钱和库存的几个页面（预收款、财务记账、退库）还是"客户端直写数据库"的老模式，网络一抖就可能保存失败，这批需要逐步收编到服务端。

上次体检发现的安全问题（12 张表没装门禁、类型安检关闭、认证文件臃肿）都已修好，说明整改方向是对的，继续保持"发现一个收编一个"的节奏即可。
