# 换电脑接手指南（2026-08-27 写）

> 场景：换一台电脑继续开发/维护本系统。按顺序做，每步做完打勾。

## 新电脑要装的软件

1. **Node.js**（LTS 版，https://nodejs.org）
2. **Git**（https://git-scm.com）
3. **Claude Code**（命令行：`npm i -g @anthropic-ai/claude-code`）
4. （仅部署生产的那台才需要）**PM2**：`npm i -g pm2`

## 第 1 步：拿代码

```bash
git clone git@github.com:dichunli/Auto-Code.git auto-repair-shop
```
（如果克隆因网络失败，先去做第 3 步的网络配置再回来）

## 第 2 步：从旧电脑带三样东西（U 盘或微信文件传输）

| 要带的 | 旧电脑位置 | 新电脑位置 | 为什么必须带 |
|---|---|---|---|
| **`.env.local`** | 仓库根目录 | 仓库根目录 | 数据库钥匙，没它连不上数据库。⚠️ 别发微信群，里面有机密 |
| **AI 记忆文件夹** | `C:\Users\Administrator\.claude\projects\D--claude-auto-repair-shop\memory\` 整个文件夹 | 新电脑 `C:\Users\<新用户名>\.claude\projects\D--claude-auto-repair-shop\memory\`（路径随仓库所在盘符/目录变化，比如仓库放 E 盘就是 `E--claude-auto-repair-shop`） | 这是 AI 的"项目经验"：所有踩过的坑、设计方案、工作流。带了它，新会话的 AI 立刻知道全部历史 |
| （可选）调试工具脚本 | 仓库根目录的 check-*.js | 已在仓库里，git pull 就有 | 不用带 |

> 如果仓库在新电脑放的路径不一样（比如从 D:\claude 换到 C:\projects），记忆文件夹名按新路径换算：把 `D:\claude\auto-repair-shop` 里的 `:\` 和 `\` 换成 `-`，即 `D--claude-auto-repair-shop`。

## 第 3 步：配 GitHub 网络（本网络环境特有，不配推拉不了代码）

1. **改 hosts**（C:\Windows\System32\drivers\etc\hosts，管理员权限打开），加一行：
   ```
   20.205.243.168 api.github.com
   ```
   （这个 IP 失效了就用 `curl "https://dns.alidns.com/resolve?name=api.github.com&type=A"` 重查一个可用的换上）

2. **写 SSH 配置**（新建/编辑 `C:\Users\<用户名>\.ssh\config`）：
   ```
   Host github.com
     HostName ssh.github.com
     Port 443
   ```

3. **验证**：仓库目录里 `git pull`，能拉下来就成了。

## 第 4 步：装依赖、跑起来

```bash
cd auto-repair-shop
npm install
npm run build        # 验证能构建（或按 08-deployment 规则起 dev）
```

## 第 5 步：继续干活

打开仓库根目录的**《系统优化待办清单-2026-08.md》**，跟 AI 说"**继续待办清单**"或点名某一项即可。清单里每项都写了改哪个文件、怎么改、注意事项。

## 两个电脑分工提醒

- **生产机**（店里跑服务的 Windows 11）：代码在 `D:\claude\auto-repair-shop`（以实际为准），PM2 管服务，部署用 `deploy.bat`
- **开发机**：随便哪台，改代码、开 PR。合并后在生产机上 `git pull` + 部署
- **别在两台电脑上同时改代码不提交**——每做完一点就提交推送，另一台 pull，就不怕撞车
