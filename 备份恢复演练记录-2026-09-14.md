# 备份恢复演练记录（2026-09-14）

## 本次演练做了什么

| 验证项 | 结果 |
|---|---|
| 备份导出链路（`backup-export.js` 全库导出） | ✅ 157 张表全部导出成功，含 `_行数台账.txt` |
| 恢复脚本读取备份（`restore-import.js` 预览模式） | ✅ 正确列出目标库、表清单、行数，不碰数据 |
| 恢复脚本安全闸门 | ✅ 不传 `--确认` 一律不写入；目标表非空时主键冲突即中止（不会产生半批脏数据，每批 insert 是原子单语句） |
| lint / 单元测试 / build | ✅ 0 error / 443 通过 / 构建成功 |

## 本次没做什么（诚实记录）

- **没有做过真实写入恢复**（把数据真正灌回数据库）。原因：本机只有生产库，往生产库演练恢复属于高危操作，需要拍板。
- 完整写入恢复要等以下任一条件再演练：
  1. 在 Supabase 建一个免费测试项目（空库），跑迁移建表后完整恢复一次；或
  2. 拍板允许在生产库用一张小表跑一次（预期主键冲突报错，验证脚本能正确拒绝）。

## 真出事时的完整恢复步骤（照此执行）

1. **建库**：在 Supabase 新建项目（或确认目标库表结构已存在）。新库需先在 SQL Editor 按顺序执行 `supabase/migrations_*.sql` 建表（或等 #30 基线化后跑基线文件）。
2. **配置**：把新库的 URL 和 service_role key 写进 `.env.local`（临时改，恢复完改回）。
3. **预览**：`node scripts/restore-import.js <备份目录>` —— 核对目标库 URL 无误。
4. **执行**：`node scripts/restore-import.js <备份目录> --确认` —— 脚本自动多轮落位外键依赖。
5. **核对**：脚本自动逐表对比 `_行数台账.txt`，全部 ✅ 才算完。
6. **切换**：应用 `.env.local` 指向新库，重启服务，验证登录/工单/库存。

## 下次演练

- 每季度一次（与《诊断报告处理方案》长期工程对齐）。下次目标：建测试项目做**完整写入恢复**，把本记录"没做什么"一栏清零。

---

*演练人：Claude（自动） · 备份目录：D:/temp-restore-drill/2026-09-14（演练后可删）*
