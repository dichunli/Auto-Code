/* ============================================================
   其它收支 name 字段改为可选
   ============================================================ */

ALTER TABLE other_transactions ALTER COLUMN name DROP NOT NULL;
